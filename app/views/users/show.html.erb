<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
      <h2>User info</h2>
      <%= render 'info', user: @user %>
      
      <h2 class="mt-3">New book</h2>
      <%= render 'books/form', book: @book %>
    </div>
    <div class='col-md-8 offset-md-1'>
      <h2>Books</h2>
      <%= render 'books/index',books: @books %>
<!--day,weekのbook投稿数数-->
 <!--部分テンプレート化してるが、今回はbook/showのみの記述なのでテンプレート
 ページはインスタンス変数使用し、renderは変数の指定なし-->
      <%= render "users/books_counts"%>
<!--ここまで-->
    </div>
  </div>
</div>